language: node_js
node_js:
  - '0.10'
before_install:
  - currentfolder=${PWD##*/}
  - if [ "$currentfolder" != 'generator-sample' ]; then cd .. && eval "mv $currentfolder generator-sample" && cd generator-sample; fi

